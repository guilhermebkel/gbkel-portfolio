import styled from "styled-components"

export const Wave = styled.div`
	height: 12rem;
	width: 100%;

	margin: auto;

	opacity: 0.08;

	position: fixed;

	top: 0;
	bottom: 0;

	transform: rotate(90deg);

	z-index: 1;
`